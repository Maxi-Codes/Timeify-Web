/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Absence } from '../../models/absence';
import { UpdateAbsenceStatusDto } from '../../models/update-absence-status-dto';

export interface ApiAbsencesIdStatusPatch$Plain$Params {
  id: string;
      body?: UpdateAbsenceStatusDto
}

export function apiAbsencesIdStatusPatch$Plain(http: HttpClient, rootUrl: string, params: ApiAbsencesIdStatusPatch$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Absence>> {
  const rb = new RequestBuilder(rootUrl, apiAbsencesIdStatusPatch$Plain.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Absence>;
    })
  );
}

apiAbsencesIdStatusPatch$Plain.PATH = '/api/absences/{id}/status';
