/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TimeEntry } from '../../models/time-entry';

export interface ApiTimeEntriesGet$Plain$Params {
  userId?: string;
  projectId?: string;
  from?: string;
  to?: string;
}

export function apiTimeEntriesGet$Plain(http: HttpClient, rootUrl: string, params?: ApiTimeEntriesGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TimeEntry>>> {
  const rb = new RequestBuilder(rootUrl, apiTimeEntriesGet$Plain.PATH, 'get');
  if (params) {
    rb.query('userId', params.userId, {});
    rb.query('projectId', params.projectId, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TimeEntry>>;
    })
  );
}

apiTimeEntriesGet$Plain.PATH = '/api/time-entries';
