/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Role } from '../../models/role';
import { UserResponseDto } from '../../models/user-response-dto';

export interface ApiUsersGet$Json$Params {
  companyId?: string;
  role?: Role;
}

export function apiUsersGet$Json(http: HttpClient, rootUrl: string, params?: ApiUsersGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<UserResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, apiUsersGet$Json.PATH, 'get');
  if (params) {
    rb.query('companyId', params.companyId, {});
    rb.query('role', params.role, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<UserResponseDto>>;
    })
  );
}

apiUsersGet$Json.PATH = '/api/users';
